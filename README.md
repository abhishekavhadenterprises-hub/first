
  # Student M (changes ABHI)

  This is a code bundle for Student M (changes ABHI). The original project is available at https://www.figma.com/design/abeKCWWoF8l4DaaGwkeImi/Student-M--changes-ABHI-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  